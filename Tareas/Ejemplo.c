#include <stdio.h>
int mul(int, int);   /* prototipo */
void main (void)  /* no recibe nada ni devuelve nada la función principal (main()) */
{
int x=10, y=20, z;
 /* 1 */
printf("\nEl resultado es: %d",x*y); /* 2 */
z=mul(x,y);
}
mul(y,x)
    {
        for (i)
         printf("\n %d", x*y);
    }
